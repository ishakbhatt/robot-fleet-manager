# Robot Fleet Manager via REST
Coding exercise: Sample Robot Fleet Manager that publishes robot health information to the web via REST API calls. 

## Future developments
- get sensing info via sensor SDKs, mcap api, and sensor web pages
